name: "테스트용"
on:
  push:
    branches:
      - 'main'

jobs:
  try_test:
    if: ${{ github.event.label.name == 'test' }}
    name: test
    steps:
      - name: Test
        run: |
          echo ${{ github.event.label.name }}
